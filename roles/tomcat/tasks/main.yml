--- 
- name: Install Java
  yum: name=java-1.8.0-openjdk state=present

- name: add group "tomcat"
  group: name=tomcat

- name: add user "tomcat"
  user: name=tomcat group=tomcat home=/usr/share/tomcat createhome=no
  become: True
  become_method: sudo

- name: Download Tomcat
  get_url: url=https://archive.apache.org/dist/tomcat/tomcat-8/v8.5.45/bin/apache-tomcat-8.5.45.tar.gz dest=/opt/apache-tomcat-8.5.45.tar.gz

- name: Extract archive
  command: chdir=/usr/share /bin/tar xvf /opt/apache-tomcat-8.5.45.tar.gz -C /opt/ creates=/opt/apache-tomcat-8.5.45

- name: Symlink install directory
  file: src=/opt/apache-tomcat-8.5.45 path=/usr/share/tomcat state=link

- name: Change ownership of Tomcat installation
  file: path=/usr/share/tomcat/ owner=tomcat group=tomcat state=directory recurse=yes

- name: Configure Tomcat server
  template: src=server.xml dest=/usr/share/tomcat/conf/

- name: Configure Tomcat Users
  template: src=tomcat-users.xml dest=/usr/share/tomcat/conf/

- name: insert firewalld rule for tomcat http port
  firewalld: port={{ http_port }}/tcp permanent=true state=enabled immediate=yes
  when: "ansible_os_family == 'RedHat' and ansible_distribution_major_version == '7'"

- name: insert firewalld rule for tomcat https port
  firewalld: port={{ https_port }}/tcp permanent=true state=enabled immediate=yes
  when: "ansible_os_family == 'RedHat' and ansible_distribution_major_version == '7'"

- name: Configure Tomcat Service file
  template:
     src: templates/tomcat.service
     dest: /etc/systemd/system/tomcat.service
     owner: root
     group: root
     mode: '0644'

- name: Reload Tomcat Service
  systemd: daemon_reload=yes

- name: Symlink for Tomcat Service
  systemd:
    name: tomcat
    enabled: yes

- name: Start the service
  systemd: state=started name=tomcat

- name: wait for tomcat to start
  wait_for: port={{http_port}}

- name: Copy sample webapp
  copy:
     src: sample.war
     dest: /opt/apache-tomcat-8.5.45/webapps/sample.war
     owner: tomcat
     group: tomcat
     mode: '0644'

- name: Mkdir for DevOps Site
  file: path=/opt/apache-tomcat-8.5.45/webapps/devops owner=tomcat group=tomcat state=directory recurse=yes

- name: Copy DevOps webapp
  copy:
     src: index.html
     dest: /opt/apache-tomcat-8.5.45/webapps/devops
     owner: tomcat
     group: tomcat
     mode: '0644'
